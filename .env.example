# CS2 Log Generator - Environment Configuration

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================

# Server Configuration
PORT=8080
ENV=development
LOG_LEVEL=debug
GIN_MODE=debug

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Demo Parser Configuration
DEMO_PARSER_TIMEOUT=30s
MAX_UPLOAD_SIZE=500MB

# Match Generation Settings
MAX_ROUNDS=30
DEFAULT_TICK_RATE=64
DEFAULT_FORMAT=MR12

# ============================================================================
# FRONTEND CONFIGURATION  
# ============================================================================

# API Configuration
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080
VITE_ENV=development

# UI Configuration
VITE_APP_TITLE="CS2 Log Generator"
VITE_APP_VERSION=1.0.0

# Feature Flags
VITE_ENABLE_DEMO_UPLOAD=true
VITE_ENABLE_REAL_TIME=true
VITE_ENABLE_STATISTICS=false

# ============================================================================
# REDIS CONFIGURATION (Optional)
# ============================================================================

REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_MAX_CONNECTIONS=10

# ============================================================================
# DEVELOPMENT CONFIGURATION
# ============================================================================

# Docker Configuration
COMPOSE_PROJECT_NAME=cs2-log-generator
COMPOSE_HTTP_TIMEOUT=120

# Hot Reload Settings
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=true

# ============================================================================
# PRODUCTION OVERRIDES
# ============================================================================
# Copy to .env.production for production builds

# Production Backend
# PORT=8080
# ENV=production
# LOG_LEVEL=info
# GIN_MODE=release
# CORS_ORIGIN=https://yourdomain.com

# Production Frontend
# VITE_API_URL=https://api.yourdomain.com
# VITE_WS_URL=wss://api.yourdomain.com
# VITE_ENV=production

# ============================================================================
# MONITORING & OBSERVABILITY
# ============================================================================

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s

# Metrics (Future)
# ENABLE_METRICS=true
# METRICS_PORT=9090

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# JWT Configuration (Future)
# JWT_SECRET=your-super-secret-jwt-key
# JWT_EXPIRY=24h

# Rate Limiting (Future)  
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS_PER_MINUTE=100

# ============================================================================
# NOTES
# ============================================================================
# 
# 1. Copy this file to .env for local development
# 2. Never commit .env files to version control
# 3. Use .env.production for production environments
# 4. Docker Compose will automatically load .env files
# 5. Vite variables must be prefixed with VITE_ to be available in frontend
# 6. Go backend reads environment variables directly
#
# For Docker development:
# - Backend runs on: http://localhost:8080
# - Frontend runs on: http://localhost:5173  
# - Redis runs on: localhost:6379 (with --profile with-cache)
#
# Health checks:
# - Backend: curl http://localhost:8080/health
# - Frontend: curl http://localhost:5173/health (production only)